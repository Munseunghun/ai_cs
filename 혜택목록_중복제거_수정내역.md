# 혜택 목록 중복 제거

## 📋 문제 상황

라이브 방송 상세 조회 페이지의 **혜택** 탭에서 여러 문제가 발생했습니다.

### 증상
1. **혜택 탭 숫자 불일치**: 탭에는 24개로 표시되지만 실제 표시되는 혜택 수와 다름
2. **할인 혜택 중복**: 동일한 할인 혜택이 여러 번 반복 표시
3. **사은품 중복**: 동일한 사은품이 여러 번 반복 표시
4. **배송 혜택 중복**: 동일한 배송 혜택이 여러 번 반복 표시

### 원인
- 데이터베이스의 `live_benefits` 테이블에 동일한 혜택이 중복 저장됨
- 백엔드에서 중복 제거 로직 없이 그대로 반환
- 크롤러에서 혜택 데이터 수집 시 중복 체크 미흡

## ✅ 해결 방법

### 백엔드 중복 제거 로직 추가

모든 혜택 타입(할인, 사은품, 쿠폰, 배송)에 대해 중복을 제거하는 로직을 추가했습니다.

#### 중복 판단 기준
다음 필드들이 모두 동일한 경우 중복으로 판단:
1. `benefit_type` - 혜택 타입 (할인, 사은품, 쿠폰, 배송)
2. `benefit_name` - 혜택명
3. `benefit_detail` - 혜택 상세 내용
4. `benefit_condition` - 적용 조건
5. `benefit_valid_period` - 유효 기간

#### 구현 코드

```javascript
// 혜택 중복 제거 함수
const removeDuplicateBenefits = (p_benefits) => {
  const _v_unique = [];
  const _v_keys = new Set();
  
  if (p_benefits && Array.isArray(p_benefits)) {
    for (const _v_benefit of p_benefits) {
      // 중복 체크를 위한 고유 키 생성
      const _v_unique_key = [
        _v_benefit.benefit_type || '',
        _v_benefit.benefit_name || '',
        _v_benefit.benefit_detail || '',
        _v_benefit.benefit_condition || '',
        _v_benefit.benefit_valid_period || ''
      ].join('|');
      
      // 중복되지 않은 경우에만 추가
      if (!_v_keys.has(_v_unique_key)) {
        _v_keys.add(_v_unique_key);
        _v_unique.push(_v_benefit);
      }
    }
  }
  
  return _v_unique;
};

// 각 혜택 타입별로 중복 제거
const _v_discounts = removeDuplicateBenefits(_v_discounts_raw);
const _v_gifts = removeDuplicateBenefits(_v_gifts_raw);
const _v_coupons = removeDuplicateBenefits(_v_coupons_raw);
const _v_shipping = removeDuplicateBenefits(_v_shipping_raw);
```

#### 로깅 개선

각 혜택 타입별로 중복 제거 결과를 로그에 기록:

```javascript
logger.info('관련 데이터 조회 완료:', {
  // 할인 혜택
  discounts_raw: _v_discounts_raw?.length || 0,
  discounts_unique: _v_discounts.length,
  discounts_duplicates_removed: (_v_discounts_raw?.length || 0) - _v_discounts.length,
  
  // 사은품
  gifts_raw: _v_gifts_raw?.length || 0,
  gifts_unique: _v_gifts.length,
  gifts_duplicates_removed: (_v_gifts_raw?.length || 0) - _v_gifts.length,
  
  // 쿠폰
  coupons_raw: _v_coupons_raw?.length || 0,
  coupons_unique: _v_coupons.length,
  coupons_duplicates_removed: (_v_coupons_raw?.length || 0) - _v_coupons.length,
  
  // 배송 혜택
  shipping_raw: _v_shipping_raw?.length || 0,
  shipping_unique: _v_shipping.length,
  shipping_duplicates_removed: (_v_shipping_raw?.length || 0) - _v_shipping.length
});
```

## 🔧 기술적 세부사항

### 1. Set 자료구조 사용
- JavaScript의 `Set`을 사용하여 O(1) 시간 복잡도로 중복 체크
- 메모리 효율적이고 빠른 성능

### 2. 고유 키 생성 전략
- 여러 필드를 파이프(`|`)로 구분하여 고유 키 생성
- 빈 값은 빈 문자열로 처리하여 null/undefined 안전성 확보
- 모든 필드가 일치해야 중복으로 판단

### 3. 타입별 독립적 처리
- 할인, 사은품, 쿠폰, 배송 각각 독립적으로 중복 제거
- 같은 함수를 재사용하여 코드 중복 최소화

### 4. 순서 보장
- 원본 데이터의 순서를 유지 (첫 번째 등장한 혜택만 유지)

## 📊 예상 결과

### 할인 혜택 - 개선 전
```
할인 혜택 (8개)
1. 슈퍼 바이탈 에센스 30% 할인
2. 슈퍼 바이탈 에센스 30% 할인  ← 중복
3. 슈퍼 바이탈 에센스 30% 할인  ← 중복
... (8번 반복)
```

### 할인 혜택 - 개선 후
```
할인 혜택 (1개)
1. 슈퍼 바이탈 에센스 30% 할인
```

### 사은품 - 개선 전
```
사은품 (6개)
1. 미니어처 세트
2. 미니어처 세트  ← 중복
3. 미니어처 세트  ← 중복
... (6번 반복)
```

### 사은품 - 개선 후
```
사은품 (1개)
1. 미니어처 세트
```

### 배송 혜택 - 개선 전
```
배송 혜택 (4개)
1. 무료배송
2. 무료배송  ← 중복
3. 무료배송  ← 중복
4. 무료배송  ← 중복
```

### 배송 혜택 - 개선 후
```
배송 혜택 (1개)
1. 무료배송
```

## 🔍 로그 확인

백엔드 로그에서 각 혜택 타입별 중복 제거 결과를 확인할 수 있습니다:

```json
{
  "discounts_raw": 8,
  "discounts_unique": 1,
  "discounts_duplicates_removed": 7,
  
  "gifts_raw": 6,
  "gifts_unique": 1,
  "gifts_duplicates_removed": 5,
  
  "coupons_raw": 4,
  "coupons_unique": 2,
  "coupons_duplicates_removed": 2,
  
  "shipping_raw": 4,
  "shipping_unique": 1,
  "shipping_duplicates_removed": 3
}
```

### 혜택 탭 숫자 계산
중복 제거 후 실제 혜택 수:
- 할인: 1개
- 사은품: 1개
- 쿠폰: 2개
- 배송: 1개
- **총 5개** (기존 24개에서 대폭 감소)

## 🚀 배포 방법

### 백엔드 재시작
```bash
cd backend
pkill -f "node.*src/server.js"
node src/server.js &
```

### 확인 방법
1. 라이브 방송 목록 페이지 접속
2. 혜택이 중복되어 있던 라이브 방송 클릭
3. **"혜택"** 탭 확인
4. 중복된 혜택이 제거되고 고유한 혜택만 표시되는지 확인
5. 혜택 탭의 숫자가 실제 표시되는 혜택 수와 일치하는지 확인

### 로그 확인
```bash
# 백엔드 로그에서 중복 제거 결과 확인
tail -f backend/logs/combined.log | grep "duplicates_removed"
```

## ⚠️ 주의사항

### 1. 조건이 다른 혜택은 별도로 취급
- 같은 할인이지만 **적용 조건**이 다르면 별도 혜택으로 표시
- 예: "3만원 이상 구매 시 30% 할인"과 "5만원 이상 구매 시 30% 할인"은 다른 혜택

### 2. 유효 기간이 다른 혜택은 별도로 취급
- 같은 혜택이지만 **유효 기간**이 다르면 별도 혜택으로 표시
- 예: "방송 중 30% 할인"과 "방송 후 1주일간 30% 할인"은 다른 혜택

### 3. 혜택 상세 내용이 다르면 별도로 취급
- 같은 혜택명이지만 **상세 내용**이 다르면 별도 혜택으로 표시
- 예: "30% 할인 (최대 1만원)"과 "30% 할인 (최대 2만원)"은 다른 혜택

## 📝 추가 개선 사항

### 1. 크롤러 단계에서 중복 방지
현재는 백엔드에서 중복을 제거하지만, 근본적으로는 크롤러 단계에서 중복 데이터가 저장되지 않도록 개선 필요:

```python
# crawler/crawlers/naver_shopping_live.py
def save_benefits(live_id, benefits):
    """혜택 데이터 저장 시 중복 체크"""
    for benefit in benefits:
        # 기존 혜택 존재 여부 확인
        existing = supabase.table('live_benefits') \
            .select('benefit_id') \
            .eq('live_id', live_id) \
            .eq('benefit_type', benefit['benefit_type']) \
            .eq('benefit_name', benefit['benefit_name']) \
            .eq('benefit_detail', benefit['benefit_detail']) \
            .eq('benefit_condition', benefit.get('benefit_condition', '')) \
            .execute()
        
        # 중복이 없는 경우에만 저장
        if not existing.data:
            supabase.table('live_benefits').insert(benefit).execute()
```

### 2. 데이터베이스 UNIQUE 제약조건
`live_benefits` 테이블에 UNIQUE 제약조건 추가:

```sql
-- live_benefits 테이블에 복합 UNIQUE 제약조건 추가
ALTER TABLE live_benefits
ADD CONSTRAINT unique_benefit_per_live
UNIQUE (live_id, benefit_type, benefit_name, benefit_detail, benefit_condition);
```

### 3. 중복 데이터 정리 스크립트
기존에 저장된 중복 데이터를 정리하는 스크립트:

```javascript
// scripts/cleanup-duplicate-benefits.js
async function cleanupDuplicateBenefits() {
  // 모든 라이브 방송 조회
  const { data: lives } = await supabase
    .from('live_broadcasts')
    .select('live_id');
  
  for (const live of lives) {
    // 각 라이브의 혜택 조회
    const { data: benefits } = await supabase
      .from('live_benefits')
      .select('*')
      .eq('live_id', live.live_id)
      .order('created_at', { ascending: true });
    
    // 중복 제거 (첫 번째 것만 유지)
    const uniqueBenefits = new Map();
    const duplicateIds = [];
    
    for (const benefit of benefits) {
      const key = `${benefit.benefit_type}|${benefit.benefit_name}|${benefit.benefit_detail}|${benefit.benefit_condition}`;
      
      if (uniqueBenefits.has(key)) {
        duplicateIds.push(benefit.benefit_id);
      } else {
        uniqueBenefits.set(key, benefit);
      }
    }
    
    // 중복 데이터 삭제
    if (duplicateIds.length > 0) {
      await supabase
        .from('live_benefits')
        .delete()
        .in('benefit_id', duplicateIds);
      
      console.log(`Live ${live.live_id}: ${duplicateIds.length}개 중복 제거`);
    }
  }
}
```

### 4. 프론트엔드 필터링 (백업)
백엔드에서 중복이 제거되지 않은 경우를 대비한 프론트엔드 필터링:

```javascript
// frontend/src/pages/LiveBroadcastDetail.jsx
const uniqueBenefits = useMemo(() => {
  if (!liveData.benefits) return { discounts: [], gifts: [], coupons: [], shipping: [] };
  
  const removeDuplicates = (benefits) => {
    const seen = new Set();
    return benefits.filter(benefit => {
      const key = `${benefit.benefit_type}|${benefit.benefit_name}|${benefit.benefit_detail}|${benefit.benefit_condition}`;
      if (seen.has(key)) return false;
      seen.add(key);
      return true;
    });
  };
  
  return {
    discounts: removeDuplicates(liveData.benefits.discounts || []),
    gifts: removeDuplicates(liveData.benefits.gifts || []),
    coupons: removeDuplicates(liveData.benefits.coupons || []),
    shipping: removeDuplicates(liveData.benefits.shipping || [])
  };
}, [liveData.benefits]);
```

### 5. 혜택 집계 기능
같은 혜택이 여러 번 등장하는 경우, 횟수를 표시:

```javascript
// 같은 혜택의 등장 횟수 집계
const _v_aggregated_benefits = [];
const _v_benefit_map = new Map();

for (const _v_benefit of _v_benefits) {
  const _v_key = `${_v_benefit.benefit_type}|${_v_benefit.benefit_name}`;
  
  if (_v_benefit_map.has(_v_key)) {
    // 기존 혜택의 등장 횟수 증가
    const _v_existing = _v_benefit_map.get(_v_key);
    _v_existing.count = (_v_existing.count || 1) + 1;
  } else {
    // 새 혜택 추가
    _v_benefit_map.set(_v_key, { ..._v_benefit, count: 1 });
  }
}

_v_aggregated_benefits = Array.from(_v_benefit_map.values());
```

## 🎯 기대 효과

1. **정확한 정보 제공**: 중복 없이 실제 혜택 목록만 표시
2. **혜택 탭 숫자 정확성**: 탭의 숫자와 실제 표시되는 혜택 수 일치
3. **사용자 경험 개선**: 혼란스러운 중복 정보 제거
4. **데이터 신뢰성 향상**: 정확한 혜택 수 표시
5. **성능 개선**: 불필요한 중복 데이터 전송 감소
6. **유지보수 용이**: 명확한 중복 제거 로직

## 📁 수정된 파일

- `/backend/src/services/eventService.js` (혜택 중복 제거 로직 추가)

## 🔍 테스트 시나리오

### 1. 모든 혜택이 중복인 경우
- **입력**: 할인 8개, 사은품 6개, 배송 4개 (모두 동일)
- **출력**: 할인 1개, 사은품 1개, 배송 1개
- **혜택 탭 숫자**: 3개

### 2. 부분 중복이 있는 경우
- **입력**: 할인 A 3개, 할인 B 2개, 사은품 C 4개
- **출력**: 할인 A 1개, 할인 B 1개, 사은품 C 1개
- **혜택 탭 숫자**: 3개

### 3. 중복이 없는 경우
- **입력**: 서로 다른 혜택 5개
- **출력**: 5개 혜택 그대로
- **혜택 탭 숫자**: 5개

### 4. 조건이 다른 경우
- **입력**: 같은 할인, 다른 조건 (예: 구매 금액 조건 다름)
- **출력**: 각 조건별로 1개씩 표시
- **로그**: 중복으로 판단하지 않음

## 📊 성능 영향

- **시간 복잡도**: O(n) - n은 혜택 수
- **공간 복잡도**: O(n) - Set에 고유 키 저장
- **실제 영향**: 혜택 수가 일반적으로 수십 개 이하이므로 성능 영향 미미

---

**작성일**: 2025-12-03  
**작성자**: AI Assistant  
**관련 이슈**: 혜택 목록에서 동일한 혜택이 중복 표시되고, 혜택 탭 숫자가 불일치  
**버전**: v1.0
