# 백엔드 배포 가이드

작성 일시: 2025-12-04 16:10

---

## 🚨 현재 문제

프론트엔드가 Firebase에 배포되었지만, 백엔드는 로컬(`http://localhost:3001`)에서만 실행 중입니다.

**에러 메시지**:
```
데이터 로드 실패
백엔드 서버에 연결할 수 없습니다. 서버가 실행 중인지 확인해주세요. (Failed to fetch)
```

**원인**: 다른 PC에서는 `localhost:3001`에 접근할 수 없음

---

## 🎯 해결 방안

### 옵션 1: Render.com 배포 (무료, 권장)

#### 1-1. Render 계정 생성
1. https://render.com 접속
2. GitHub 계정으로 로그인
3. 새 Web Service 생성

#### 1-2. 백엔드 배포 설정

**Build Command**:
```bash
npm install
```

**Start Command**:
```bash
npm start
```

**Environment Variables** (Render 대시보드에서 설정):
```env
NODE_ENV=production
PORT=3001
SUPABASE_URL=https://uewhvekfjjvxoioklzza.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVld2h2ZWtmamp2eG9pb2tsenphIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQzNDI5NDYsImV4cCI6MjA3OTkxODk0Nn0.bMLOKKMLyz7VEr3B8IMo-upyZ4rzvzm3NSZYLfkYU3I
REDIS_URL=redis://localhost:6379
```

#### 1-3. 배포 후 URL 획득

배포 완료 후 Render가 제공하는 URL 예시:
```
https://your-app-name.onrender.com
```

#### 1-4. 프론트엔드 환경 변수 설정

**파일**: `frontend/.env.production` (새로 생성)

```env
# Supabase 설정
REACT_APP_SUPABASE_URL=https://uewhvekfjjvxoioklzza.supabase.co
REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVld2h2ZWtmamp2eG9pb2tsenphIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQzNDI5NDYsImV4cCI6MjA3OTkxODk0Nn0.bMLOKKMLyz7VEr3B8IMo-upyZ4rzvzm3NSZYLfkYU3I

# 백엔드 API URL (Render에서 받은 URL로 변경)
REACT_APP_API_URL=https://your-app-name.onrender.com

# Firebase 설정
REACT_APP_FIREBASE_API_KEY=AIzaSyDq11o7vR0PNiMm7gutKPQGlmFOCvcNtis
REACT_APP_FIREBASE_AUTH_DOMAIN=ai-cs-bf933.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=ai-cs-bf933
REACT_APP_FIREBASE_STORAGE_BUCKET=ai-cs-bf933.firebasestorage.app
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=209696960764
REACT_APP_FIREBASE_APP_ID=1:209696960764:web:cbdb5dbb11bf3da4cd6779

# Supabase Publishable Key
REACT_APP_SUPABASE_PUBLISHABLE_KEY=sb_publishable_CLrBJ-Hxb7h3sKNUgW08Zg_M6UFo1kN
```

#### 1-5. 프론트엔드 재빌드 및 재배포

```bash
# 프론트엔드 빌드
cd frontend
npm run build

# Firebase 재배포
cd ..
firebase deploy --only hosting
```

---

### 옵션 2: Railway 배포 (무료 티어 제한적)

#### 2-1. Railway 계정 생성
1. https://railway.app 접속
2. GitHub 계정으로 로그인

#### 2-2. 프로젝트 생성
1. "New Project" 클릭
2. "Deploy from GitHub repo" 선택
3. 백엔드 폴더 선택

#### 2-3. 환경 변수 설정
Railway 대시보드에서 동일한 환경 변수 설정

#### 2-4. 배포 URL 획득 및 프론트엔드 설정
옵션 1과 동일한 프로세스

---

### 옵션 3: Heroku 배포 (유료)

#### 3-1. Heroku CLI 설치
```bash
brew tap heroku/brew && brew install heroku
```

#### 3-2. Heroku 로그인
```bash
heroku login
```

#### 3-3. 앱 생성 및 배포
```bash
cd backend
heroku create your-app-name
git push heroku main
```

#### 3-4. 환경 변수 설정
```bash
heroku config:set NODE_ENV=production
heroku config:set SUPABASE_URL=https://uewhvekfjjvxoioklzza.supabase.co
heroku config:set SUPABASE_ANON_KEY=your-key
```

---

### 옵션 4: 로컬 네트워크 내 접근 (임시 해결)

**주의**: 같은 네트워크에 있을 때만 작동

#### 4-1. 로컬 IP 확인
```bash
# Mac/Linux
ifconfig | grep "inet "

# Windows
ipconfig
```

예시 결과: `192.168.1.100`

#### 4-2. 백엔드 서버 외부 접근 허용

**파일**: `backend/src/server.js` 수정

```javascript
const PORT = process.env.PORT || 3001;
const HOST = '0.0.0.0'; // 모든 네트워크 인터페이스에서 접근 허용

app.listen(PORT, HOST, () => {
  logger.info(`Server is running on http://${HOST}:${PORT}`);
});
```

#### 4-3. 프론트엔드 환경 변수 설정

**파일**: `frontend/.env.production`

```env
REACT_APP_API_URL=http://192.168.1.100:3001
```

#### 4-4. 방화벽 설정
```bash
# Mac
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /path/to/node
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /path/to/node
```

**단점**:
- 같은 네트워크에서만 작동
- IP 주소가 변경될 수 있음
- 외부 인터넷에서 접근 불가

---

## 🎯 권장 사항

**Render.com 배포 (옵션 1)** 추천 이유:
- ✅ 무료 티어 제공
- ✅ GitHub 연동 자동 배포
- ✅ HTTPS 자동 제공
- ✅ 환경 변수 관리 쉬움
- ✅ 로그 모니터링 제공
- ✅ 외부 인터넷에서 접근 가능

---

## 📝 배포 후 체크리스트

- [ ] 백엔드 클라우드 배포 완료
- [ ] 백엔드 URL 획득
- [ ] `frontend/.env.production` 파일 생성
- [ ] `REACT_APP_API_URL` 설정
- [ ] 프론트엔드 재빌드
- [ ] Firebase 재배포
- [ ] 다른 PC에서 접속 테스트
- [ ] API 호출 정상 작동 확인

---

## 🔧 CORS 설정 확인

백엔드에서 CORS 설정이 필요할 수 있습니다.

**파일**: `backend/src/server.js`

```javascript
const cors = require('cors');

app.use(cors({
  origin: [
    'http://localhost:3000',
    'https://ai-cs-bf933.web.app',
    'https://ai-cs-bf933.firebaseapp.com'
  ],
  credentials: true
}));
```

---

## 🚀 빠른 시작 (Render 배포)

### 1단계: Render에 백엔드 배포
```bash
# 1. https://render.com 접속
# 2. GitHub 연동
# 3. backend 폴더 선택
# 4. 환경 변수 설정
# 5. 배포 완료 후 URL 복사
```

### 2단계: 프론트엔드 환경 변수 설정
```bash
# frontend/.env.production 파일 생성
echo "REACT_APP_API_URL=https://your-app.onrender.com" > frontend/.env.production
```

### 3단계: 재배포
```bash
cd frontend
npm run build
cd ..
firebase deploy --only hosting
```

---

## ✅ 완료!

백엔드가 클라우드에 배포되고 프론트엔드가 해당 URL을 참조하면, 모든 PC에서 정상적으로 접근할 수 있습니다.
