# 혜택 정보 & 사은품 표시 수정 완료

작성 일시: 2025-12-04 09:35

---

## ✅ 수정 완료

### 문제점
- 혜택 정보와 사은품 정보가 데이터가 없으면 아예 표시되지 않음
- 사용자가 해당 섹션의 존재를 알 수 없음

### 해결 방법
- **데이터가 없어도 섹션은 항상 표시**
- **개수를 0으로 표시**
- **"정보가 없습니다" 메시지 표시**

---

## 📊 수정된 섹션 (4개)

### 1. 할인 혜택
**변경 전**:
```jsx
{liveData.benefits?.discounts && liveData.benefits.discounts.length > 0 && (
  <Paper>
    <Typography>할인 혜택 ({liveData.benefits.discounts.length}개)</Typography>
    {/* 데이터 표시 */}
  </Paper>
)}
```

**변경 후**:
```jsx
<Paper sx={{ p: 3, mb: 2 }}>
  <Typography variant="h6" gutterBottom fontWeight="bold">
    <LocalOfferIcon /> 할인 혜택 ({liveData.benefits?.discounts?.length || 0}개)
  </Typography>
  <Divider sx={{ mb: 2 }} />
  
  {liveData.benefits?.discounts && liveData.benefits.discounts.length > 0 ? (
    liveData.benefits.discounts.map((discount, index) => (
      <Card key={index}>
        <CardContent>
          <Chip label={discount.benefit_name || '할인'} />
          <Typography>{discount.benefit_detail || '-'}</Typography>
          {discount.benefit_condition && (
            <Typography variant="caption">
              조건: {discount.benefit_condition}
            </Typography>
          )}
          {discount.benefit_valid_period && (
            <Typography variant="caption">
              유효기간: {discount.benefit_valid_period}
            </Typography>
          )}
        </CardContent>
      </Card>
    ))
  ) : (
    <Alert severity="info">할인 혜택이 없습니다.</Alert>
  )}
</Paper>
```

**주요 변경사항**:
- ✅ 섹션 항상 표시
- ✅ 개수 0으로 표시
- ✅ 데이터 없으면 "할인 혜택이 없습니다" 메시지
- ✅ `benefit_name`, `benefit_detail`, `benefit_condition`, `benefit_valid_period` 필드 매핑

---

### 2. 사은품
**변경 전**:
```jsx
{liveData.benefits?.gifts && liveData.benefits.gifts.length > 0 && (
  <Paper>
    <Typography>사은품 ({liveData.benefits.gifts.length}개)</Typography>
    {/* 데이터 표시 */}
  </Paper>
)}
```

**변경 후**:
```jsx
<Paper sx={{ p: 3, mb: 2 }}>
  <Typography variant="h6" gutterBottom fontWeight="bold">
    <CardGiftcardIcon /> 사은품 ({liveData.benefits?.gifts?.length || 0}개)
  </Typography>
  <Divider sx={{ mb: 2 }} />
  
  {liveData.benefits?.gifts && liveData.benefits.gifts.length > 0 ? (
    liveData.benefits.gifts.map((gift, index) => (
      <Card key={index}>
        <CardContent>
          <Chip label={gift.benefit_name || gift.gift_name || '사은품'} />
          {(gift.quantity_limit || gift.quantity_limit_text) && (
            <Chip label={gift.quantity_limit} color="warning" />
          )}
          <Typography variant="body1" fontWeight="bold">
            {gift.benefit_name || gift.gift_name || '사은품'}
          </Typography>
          <Typography variant="body2">
            {gift.benefit_detail || gift.gift_detail || '-'}
          </Typography>
          {(gift.benefit_condition || gift.gift_condition) && (
            <Typography variant="body2" color="text.secondary">
              조건: {gift.benefit_condition || gift.gift_condition}
            </Typography>
          )}
        </CardContent>
      </Card>
    ))
  ) : (
    <Alert severity="info">사은품이 없습니다.</Alert>
  )}
</Paper>
```

**주요 변경사항**:
- ✅ 섹션 항상 표시
- ✅ 개수 0으로 표시
- ✅ 데이터 없으면 "사은품이 없습니다" 메시지
- ✅ `benefit_name`, `benefit_detail`, `benefit_condition` 필드 매핑
- ✅ 기존 `gift_name`, `gift_detail`, `gift_condition` 필드도 호환

---

### 3. 쿠폰/적립
**변경 전**:
```jsx
{liveData.benefits?.coupons && liveData.benefits.coupons.length > 0 && (
  <Paper>
    <Typography>🎟️ 쿠폰/적립 ({liveData.benefits.coupons.length}개)</Typography>
    {/* 데이터 표시 */}
  </Paper>
)}
```

**변경 후**:
```jsx
<Paper sx={{ p: 3, mb: 2 }}>
  <Typography variant="h6" gutterBottom fontWeight="bold">
    🎟️ 쿠폰/적립 ({liveData.benefits?.coupons?.length || 0}개)
  </Typography>
  <Divider sx={{ mb: 2 }} />
  
  {liveData.benefits?.coupons && liveData.benefits.coupons.length > 0 ? (
    liveData.benefits.coupons.map((coupon, index) => (
      <Card key={index}>
        <CardContent>
          <Chip label={coupon.benefit_name || coupon.coupon_type || '쿠폰'} />
          <Typography variant="h6">
            {coupon.benefit_detail || coupon.coupon_value || '-'}
          </Typography>
          <Typography variant="body1" fontWeight="bold">
            {coupon.benefit_name || coupon.coupon_name || '쿠폰'}
          </Typography>
          {(coupon.benefit_condition || coupon.issue_condition) && (
            <Typography variant="caption">
              조건: {coupon.benefit_condition || coupon.issue_condition}
            </Typography>
          )}
          {(coupon.benefit_valid_period || coupon.reward_detail) && (
            <Typography variant="caption" color="primary">
              {coupon.benefit_valid_period || coupon.reward_detail}
            </Typography>
          )}
        </CardContent>
      </Card>
    ))
  ) : (
    <Alert severity="info">쿠폰이 없습니다.</Alert>
  )}
</Paper>
```

**주요 변경사항**:
- ✅ 섹션 항상 표시
- ✅ 개수 0으로 표시
- ✅ 데이터 없으면 "쿠폰이 없습니다" 메시지
- ✅ `benefit_name`, `benefit_detail`, `benefit_condition`, `benefit_valid_period` 필드 매핑

---

### 4. 배송 혜택
**변경 전**:
```jsx
{liveData.benefits?.delivery && liveData.benefits.delivery.length > 0 && (
  <Paper>
    <Typography>배송 혜택 ({liveData.benefits.delivery.length}개)</Typography>
    {/* 데이터 표시 */}
  </Paper>
)}
```

**변경 후**:
```jsx
<Paper sx={{ p: 3, mb: 3 }}>
  <Typography variant="h6" gutterBottom fontWeight="bold">
    <LocalShippingIcon /> 배송 혜택 
    ({liveData.benefits?.delivery?.length || liveData.benefits?.shipping?.length || 0}개)
  </Typography>
  <Divider sx={{ mb: 2 }} />
  
  {(liveData.benefits?.delivery && liveData.benefits.delivery.length > 0) || 
   (liveData.benefits?.shipping && liveData.benefits.shipping.length > 0) ? (
    (liveData.benefits?.delivery || liveData.benefits?.shipping || []).map((shipping, index) => (
      <Alert key={index} severity="info">
        <Typography variant="body1" fontWeight="bold">
          {shipping.benefit_name || shipping.shipping_benefit || '배송 혜택'}
        </Typography>
        <Typography variant="body2">
          {shipping.benefit_detail || shipping.shipping_detail || '-'}
        </Typography>
        {(shipping.benefit_condition || shipping.shipping_condition) && (
          <Typography variant="caption">
            조건: {shipping.benefit_condition || shipping.shipping_condition}
          </Typography>
        )}
      </Alert>
    ))
  ) : (
    <Alert severity="info">배송 혜택이 없습니다.</Alert>
  )}
</Paper>
```

**주요 변경사항**:
- ✅ 섹션 항상 표시
- ✅ 개수 0으로 표시 (`delivery` 또는 `shipping` 중 하나)
- ✅ 데이터 없으면 "배송 혜택이 없습니다" 메시지
- ✅ `benefit_name`, `benefit_detail`, `benefit_condition` 필드 매핑
- ✅ 기존 `shipping_benefit`, `shipping_detail`, `shipping_condition` 필드도 호환

---

## 🔄 데이터 필드 매핑

### live_benefits 테이블 → 프론트엔드

| DB 필드 | 프론트엔드 표시 | 용도 |
|---------|----------------|------|
| `benefit_name` | 제목/라벨 | 혜택 이름 |
| `benefit_detail` | 본문 | 혜택 상세 설명 |
| `benefit_condition` | 조건 | 적용 조건 |
| `benefit_valid_period` | 유효기간 | 혜택 유효 기간 |
| `quantity_limit` | 수량 제한 | 사은품 수량 제한 |

### 기존 필드 호환성 유지

| 기존 필드 | 신규 필드 | 비고 |
|----------|----------|------|
| `discount_name` | `benefit_name` | 할인 이름 |
| `discount_detail` | `benefit_detail` | 할인 상세 |
| `gift_name` | `benefit_name` | 사은품 이름 |
| `gift_detail` | `benefit_detail` | 사은품 상세 |
| `coupon_name` | `benefit_name` | 쿠폰 이름 |
| `coupon_value` | `benefit_detail` | 쿠폰 할인액 |
| `shipping_benefit` | `benefit_name` | 배송 혜택 이름 |
| `shipping_detail` | `benefit_detail` | 배송 혜택 상세 |

---

## 📊 표시 예시

### 데이터가 있는 경우
```
혜택 정보 (4건)

┌─────────────────────────────────────┐
│ 🏷️ 할인 혜택 (1개)                  │
├─────────────────────────────────────┤
│ [정률할인]                          │
│ 이니스프리몰 라이브 25% 할인        │
│ 조건: 라이브 방송 중 결제           │
│ 유효기간: 2025-12-01 ~ 2025-12-08   │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🎁 사은품 (0개)                     │
├─────────────────────────────────────┤
│ ℹ️ 사은품이 없습니다.               │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🎟️ 쿠폰/적립 (1개)                  │
├─────────────────────────────────────┤
│ [이니스프리몰 라이브 전용 쿠폰]    │
│ 라이브 방송 중 결제                 │
│ 조건: 라이브 방송 중 결제           │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🚚 배송 혜택 (1개)                  │
├─────────────────────────────────────┤
│ ℹ️ 무료배송                         │
│    무료배송                          │
└─────────────────────────────────────┘
```

### 데이터가 없는 경우
```
혜택 정보 (0건)

┌─────────────────────────────────────┐
│ 🏷️ 할인 혜택 (0개)                  │
├─────────────────────────────────────┤
│ ℹ️ 할인 혜택이 없습니다.            │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🎁 사은품 (0개)                     │
├─────────────────────────────────────┤
│ ℹ️ 사은품이 없습니다.               │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🎟️ 쿠폰/적립 (0개)                  │
├─────────────────────────────────────┤
│ ℹ️ 쿠폰이 없습니다.                 │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🚚 배송 혜택 (0개)                  │
├─────────────────────────────────────┤
│ ℹ️ 배송 혜택이 없습니다.            │
└─────────────────────────────────────┘
```

---

## 🧪 테스트 방법

### 1. 데이터가 있는 라이브
```bash
# 브라우저에서 접속
http://localhost:3000

# 라이브 조회 > "INNISFREE_MALL_INNISFREE_006" 검색
# 상세 보기 클릭
# 하단 스크롤하여 "혜택 정보 (4건)" 섹션 확인
```

**예상 결과**:
- ✅ 할인 혜택 (1개) - 카드 1개 표시
- ✅ 사은품 (0개) - "사은품이 없습니다" 메시지
- ✅ 쿠폰/적립 (1개) - 카드 1개 표시
- ✅ 배송 혜택 (1개) - Alert 1개 표시

### 2. 사은품이 있는 라이브
```bash
# 브라우저에서 접속
http://localhost:3000

# 라이브 조회 > "REAL_NAVER_SULWHASOO_001" 검색
# 상세 보기 클릭
# 하단 스크롤하여 "혜택 정보" 섹션 확인
```

**예상 결과**:
- ✅ 할인 혜택 (1개) - 카드 1개 표시
- ✅ 사은품 (1개) - 카드 1개 표시 ("윤조 미니어처 세트")
- ✅ 쿠폰/적립 (1개) - 카드 1개 표시
- ✅ 배송 혜택 (개수) - 데이터에 따라 표시

### 3. 백엔드 API 확인
```bash
# 할인/사은품 데이터 확인
curl http://localhost:3001/api/events/INNISFREE_MALL_INNISFREE_006 | jq '.data.benefits'

# 예상 결과
{
  "discounts": [{ "benefit_name": "정률할인", ... }],
  "gifts": [],
  "coupons": [{ "benefit_name": "이니스프리몰 라이브 전용 쿠폰", ... }],
  "shipping": [{ "benefit_name": "무료배송", ... }],
  "delivery": [{ "benefit_name": "무료배송", ... }],
  "point_details": [{ "benefit_name": "이니스프리 포인트 10% 적립", ... }]
}
```

---

## ✅ 완료 체크리스트

- [x] 할인 혜택 섹션 항상 표시
- [x] 사은품 섹션 항상 표시
- [x] 쿠폰/적립 섹션 항상 표시
- [x] 배송 혜택 섹션 항상 표시
- [x] 데이터 없으면 0개 표시
- [x] 데이터 없으면 "정보가 없습니다" 메시지
- [x] `benefit_name`, `benefit_detail`, `benefit_condition`, `benefit_valid_period` 필드 매핑
- [x] 기존 필드 호환성 유지 (`gift_name`, `coupon_name`, `shipping_benefit` 등)
- [x] 총 혜택 개수 계산 (할인 + 사은품 + 쿠폰 + 배송)

---

## 🎉 완료!

혜택 정보와 사은품 정보가 데이터 유무와 관계없이 항상 표시됩니다!

**확인 방법**:
1. http://localhost:3000 접속
2. 라이브 조회 페이지에서 라이브 선택
3. 상세 보기 클릭
4. 하단 스크롤하여 "혜택 정보" 섹션 확인
5. 할인 혜택, 사은품, 쿠폰/적립, 배송 혜택 4개 섹션 모두 표시 확인
