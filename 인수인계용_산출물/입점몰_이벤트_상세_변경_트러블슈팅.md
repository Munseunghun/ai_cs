# 입점몰 이벤트 상세 화면 변경 트러블슈팅

**작성일**: 2025-12-16  
**프로젝트**: AI CS 시스템  
**상태**: 🔧 트러블슈팅 진행 중

---

## 🚨 문제 상황

### 증상
```
✅ 코드 수정 완료: LiveBroadcastDetail.jsx에서 "라이브 방송 보기" 버튼 제거
✅ Git 커밋 및 푸시 완료
✅ Netlify 재배포 트리거 완료
❌ 웹사이트에 변경사항이 반영되지 않음
```

### 확인된 페이지
```
입점몰 이벤트, 전시조회 > 상세조회
URL: https://aics1.netlify.app/live/NAVER_SHOPPING_5002337684
```

---

## 🔍 원인 분석

### 1. 페이지 경로 확인 ✅

**SearchExhibitions.jsx → LiveBroadcastDetail.jsx**

```javascript
// SearchExhibitions.jsx의 handleEventDetail 함수
const handleEventDetail = async (eventId) => {
  const _v_detail_url = `/live/${encodeURIComponent(eventId)}`;
  navigate(_v_detail_url);
};
```

**결론**: 올바른 파일(LiveBroadcastDetail.jsx)을 수정했습니다. ✅

---

### 2. 로컬 빌드 테스트 ✅

```bash
cd frontend
npm run build

결과:
✅ 빌드 성공
✅ "라이브 방송 보기" 텍스트 없음
✅ 경고만 있음 (기능에 영향 없음)
```

**결론**: 코드 수정이 정상적으로 빌드에 반영됩니다. ✅

---

### 3. 가능한 원인

#### A. Netlify 빌드 캐시 문제 ⚠️
- 이전 빌드 결과가 캐시되어 새로운 변경사항이 반영되지 않음
- `node_modules` 캐시 문제

#### B. CDN 캐시 문제 ⚠️
- Netlify CDN에 이전 버전이 캐시됨
- 캐시 TTL(Time To Live)이 만료되지 않음

#### C. 브라우저 캐시 문제 ⚠️
- 사용자 브라우저에 이전 JavaScript 파일이 캐시됨
- Service Worker 캐시

#### D. 빌드 설정 문제 ⚠️
- Base directory 설정
- Build command 설정
- Publish directory 설정

---

## ✅ 해결 방법

### 방법 1: 강제 재배포 (완료)

```bash
✅ Commit: d1518d4
✅ Message: "chore: 강제 재배포 - 캐시 클리어"
✅ Push: origin/main
⏳ Netlify 빌드 시작
```

---

### 방법 2: Netlify 대시보드에서 캐시 클리어 후 재배포 (권장)

#### Step 1: Netlify 대시보드 접속
```
https://app.netlify.com
```

#### Step 2: 사이트 선택
- **aics1** 사이트 클릭

#### Step 3: Deploys 탭
- 상단 메뉴에서 **"Deploys"** 클릭

#### Step 4: 캐시 클리어 후 재배포
```
Trigger deploy (버튼 클릭)
  ↓
Clear cache and deploy site (선택)
  ↓
Deploy site (확인)
```

**이 방법이 가장 확실합니다!**

---

### 방법 3: 빌드 설정 확인

#### Netlify Build Settings 확인

```
Site settings → Build & deploy → Build settings
```

**현재 설정 (확인 필요)**:
```
Base directory: frontend
Build command: npm install --legacy-peer-deps && npm run build
Publish directory: build
```

**올바른 설정인지 확인**:
- [ ] Base directory가 `frontend`인가?
- [ ] Build command에 `--legacy-peer-deps`가 있는가?
- [ ] Publish directory가 `build`인가?

---

### 방법 4: 환경 변수 확인

```
Site settings → Environment variables
```

**필수 환경 변수**:
```
NODE_VERSION=18
CI=false
```

---

## 🧪 테스트 방법

### 1. 브라우저 캐시 완전 삭제

#### Chrome
```
1. Ctrl + Shift + Delete (Windows)
   Cmd + Shift + Delete (Mac)

2. 시간 범위: "전체 기간"

3. 체크:
   ✅ 쿠키 및 기타 사이트 데이터
   ✅ 캐시된 이미지 및 파일

4. "데이터 삭제" 클릭
```

#### Safari
```
1. Cmd + , (환경설정)
2. 고급 탭
3. "메뉴 막대에서 개발자용 메뉴 보기" 체크
4. 개발자용 → 캐시 비우기
```

---

### 2. 시크릿 모드로 테스트

```
Chrome: Ctrl + Shift + N
Safari: Cmd + Shift + N
Firefox: Ctrl + Shift + P

→ https://aics1.netlify.app/live/NAVER_SHOPPING_5002337684
```

**시크릿 모드에서 확인**:
- 캐시가 없는 상태에서 테스트
- 가장 확실한 방법

---

### 3. 개발자 도구로 확인

```
F12 (개발자 도구)
  ↓
Network 탭
  ↓
"Disable cache" 체크
  ↓
페이지 새로고침 (Ctrl + R)
  ↓
main.*.js 파일 확인
  - Size 컬럼에서 "(disk cache)" 또는 "(memory cache)" 표시 확인
  - 새로 다운로드되었는지 확인
```

---

### 4. JavaScript 파일 직접 확인

```
1. 페이지 접속
2. F12 (개발자 도구)
3. Sources 탭
4. static/js/main.*.js 파일 열기
5. Ctrl + F로 "라이브 방송 보기" 검색
   - 없으면: 빌드가 정상 반영됨 ✅
   - 있으면: 캐시 문제 ❌
```

---

## 📊 Netlify 배포 로그 확인

### 배포 로그 보는 방법

```
Netlify 대시보드
  ↓
Deploys 탭
  ↓
최신 배포 클릭
  ↓
"Deploy log" 섹션 확인
```

---

### 정상 배포 로그

```
✅ 5:50:00 PM: Build ready to start
✅ 5:50:02 PM: Detected base directory: frontend
✅ 5:50:05 PM: Installing npm packages
✅ 5:50:06 PM: npm notice Using legacy peer deps
✅ 5:50:42 PM: added 1500 packages in 36s
✅ 5:50:43 PM: Creating an optimized production build...
✅ 5:52:25 PM: Compiled successfully.
✅ 5:52:26 PM: The build folder is ready to be deployed.
✅ 5:52:27 PM: Processing _redirects file
✅ 5:52:28 PM: Build script success
✅ 5:52:29 PM: Site is live ✨
```

**확인 사항**:
- ✅ "Compiled successfully"
- ✅ "Build script success"
- ✅ "Site is live"

---

### 실패 배포 로그

```
❌ npm error ERESOLVE could not resolve
❌ Error during npm install
❌ Failing build: Failed to install dependencies
```

**해결**:
- `.npmrc` 파일 확인
- Build command 확인

---

## 🔧 추가 해결 방법

### 방법 5: 파일명 변경으로 캐시 무효화

React 빌드 시 자동으로 파일명에 해시가 추가되므로 이미 적용되어 있습니다:
```
main.548dc2db.js  ← 해시값이 변경되면 새 파일로 인식
```

---

### 방법 6: 버전 번호 추가

`package.json`에 버전 업데이트:

```json
{
  "version": "1.0.1",  // 1.0.0 → 1.0.1
  ...
}
```

```bash
git add package.json
git commit -m "chore: 버전 업데이트 1.0.1"
git push origin main
```

---

### 방법 7: 빌드 명령어 수정

Netlify Build Settings에서:

```bash
# 기존
npm install --legacy-peer-deps && npm run build

# 수정 (캐시 무시)
rm -rf node_modules && npm install --legacy-peer-deps && npm run build
```

**주의**: 빌드 시간이 길어집니다 (약 2-3분 → 4-5분)

---

## 📱 모바일에서 확인

### iOS Safari
```
1. 설정 앱
2. Safari
3. 고급
4. 웹사이트 데이터
5. 모든 웹사이트 데이터 제거
6. Safari 앱 재시작
7. https://aics1.netlify.app/live/... 접속
```

### Android Chrome
```
1. Chrome 앱
2. 설정 (⋮)
3. 개인정보 보호 및 보안
4. 인터넷 사용 기록 삭제
5. "캐시된 이미지 및 파일" 체크
6. 데이터 삭제
7. https://aics1.netlify.app/live/... 접속
```

---

## 🎯 단계별 체크리스트

### Phase 1: 코드 확인 ✅
- [x] LiveBroadcastDetail.jsx 수정 확인
- [x] Git 커밋 확인
- [x] Git 푸시 확인
- [x] 로컬 빌드 테스트

### Phase 2: Netlify 배포 확인 ⏳
- [ ] Netlify 대시보드 접속
- [ ] 최신 배포 상태 확인 (Published?)
- [ ] 배포 로그 확인 (오류 없음?)
- [ ] 배포 시간 확인 (최근 5분 이내?)

### Phase 3: 캐시 클리어 🔄
- [ ] Netlify: "Clear cache and deploy site" 실행
- [ ] 브라우저: 캐시 완전 삭제
- [ ] 브라우저: 하드 리프레시 (Ctrl+F5)
- [ ] 시크릿 모드로 테스트

### Phase 4: 최종 확인 ✅
- [ ] "라이브 방송 보기" 버튼 없음
- [ ] "목록으로 돌아가기" 버튼만 표시
- [ ] 페이지 레이아웃 정상
- [ ] 다른 기능 정상 작동

---

## 🚀 현재 상태

### Git 커밋
```
✅ d1518d4 - chore: 강제 재배포 - 캐시 클리어
✅ f6476a1 - docs: Netlify 재배포 및 캐시 문제 해결 가이드 추가
✅ 1bd0d53 - chore: Netlify 재배포 트리거
✅ ddcefb6 - feat: 입점몰 이벤트 상세 화면에서 '라이브 방송 보기' 버튼 제거
```

### Netlify 배포
```
⏳ 빌드 진행 중
📅 시작: 방금 전
⏱️ 예상 완료: 2-4분 후
```

---

## 💡 권장 조치

### 즉시 실행 (가장 확실한 방법)

**1. Netlify에서 캐시 클리어 후 재배포**:
```
https://app.netlify.com
→ aics1 사이트
→ Deploys 탭
→ Trigger deploy
→ Clear cache and deploy site
```

**2. 배포 완료 대기** (2-4분)

**3. 브라우저 캐시 클리어**:
```
Ctrl + Shift + Delete
→ 전체 기간
→ 캐시된 이미지 및 파일
→ 삭제
```

**4. 시크릿 모드로 확인**:
```
Ctrl + Shift + N (Chrome)
→ https://aics1.netlify.app/live/NAVER_SHOPPING_5002337684
```

---

## 🐛 여전히 안 되는 경우

### 최후의 수단

#### 1. Netlify 사이트 재생성
```
1. 현재 사이트 삭제
2. 새 사이트 생성
3. GitHub 저장소 연결
4. 빌드 설정 재구성
```

#### 2. 다른 배포 플랫폼 시도
```
- Vercel
- GitHub Pages
- AWS Amplify
```

#### 3. 로컬 빌드 파일 수동 배포
```
cd frontend
npm run build
# build 폴더를 Netlify에 드래그 앤 드롭
```

---

## 📞 지원

### Netlify Support
- https://www.netlify.com/support/
- Community: https://answers.netlify.com

### 프로젝트 이슈
- GitHub: https://github.com/Munseunghun/ai_cs/issues

---

## 🎉 예상 결과

**배포 완료 후 (2-4분)**:

```
✅ https://aics1.netlify.app/live/NAVER_SHOPPING_5002337684
   - "라이브 방송 보기" 버튼 없음
   - "목록으로 돌아가기" 버튼만 표시
   - 버튼 중앙 정렬
   - 페이지 정상 작동
```

---

## ⏰ 타임라인

| 시간 | 작업 | 상태 |
|------|------|------|
| **이전** | 코드 수정 및 커밋 | ✅ 완료 |
| **방금 전** | 강제 재배포 트리거 | ✅ 완료 |
| **현재** | Netlify 빌드 중 | ⏳ 진행 중 |
| **+2분** | 빌드 완료 예상 | 대기 중 |
| **+4분** | 사이트 업데이트 | 대기 중 |

---

**작성 완료일**: 2025-12-16  
**최종 검토**: AI Assistant  
**문서 버전**: 1.0  
**상태**: 🔧 트러블슈팅 진행 중

---

## 🎯 다음 단계

1. **2-4분 대기** (Netlify 빌드 완료)
2. **Netlify 대시보드 확인** (배포 상태)
3. **브라우저 캐시 클리어** (Ctrl+Shift+Delete)
4. **시크릿 모드로 테스트** (Ctrl+Shift+N)
5. **여전히 안 되면**: "Clear cache and deploy site" 실행

---

**가장 확실한 방법: Netlify 대시보드에서 "Clear cache and deploy site"를 실행하세요!** 🚀

---

**© 2025 Amore Pacific. All Rights Reserved.**

