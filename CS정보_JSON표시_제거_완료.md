# CS 응대용 정보 - JSON 형태 데이터 표시 제거 완료

작성 일시: 2025-12-04 14:15

---

## ✅ 작업 완료

### 문제
이미지에서 확인된 것처럼 CS 응대용 정보 테이블에 JSON 형태의 원시 데이터가 그대로 표시되는 문제:
```
{"category":"방송 일정","question":"라이브 방송은 언제 시작하나요?","frequency":"high","priority":1}
```

### 목표
- JSON 문자열을 파싱하여 **질문 내용만** 깔끔하게 표시
- `category`, `frequency`, `priority` 등의 메타데이터는 별도 컬럼에 표시
- 사용자가 읽기 쉬운 형태로 변환

---

## 🎨 주요 변경 사항

### 1. 예상 고객 질문 테이블

**변경 전**:
```jsx
{_v_limited_questions.map((question, index) => {
  if (typeof question === 'object' && question !== null) {
    const _v_question_text = question.question || JSON.stringify(question);
    // ❌ JSON.stringify()로 인해 전체 객체가 문자열로 표시됨
  }
})}
```

**변경 후**:
```jsx
{_v_limited_questions.map((question, index) => {
  // ✅ 문자열인 경우 JSON 파싱 시도
  let _v_parsed_question = question;
  if (typeof question === 'string') {
    try {
      _v_parsed_question = JSON.parse(question);
    } catch (e) {
      _v_parsed_question = { question: question };
    }
  }
  
  if (typeof _v_parsed_question === 'object' && _v_parsed_question !== null) {
    // ✅ 각 필드를 개별적으로 추출
    const _v_category = _v_parsed_question.category || 
                        _v_parsed_question.question_category || '일반';
    const _v_question_text = _v_parsed_question.question || '질문 내용 없음';
    const _v_frequency = _v_parsed_question.frequency || '-';
    const _v_priority = _v_parsed_question.priority || 0;
    
    // ✅ 질문 내용만 표시
    return (
      <TableRow>
        <TableCell>{index + 1}</TableCell>
        <TableCell>{_v_category}</TableCell>
        <TableCell>{_v_question_text}</TableCell>  {/* ← 질문만 표시 */}
        <TableCell>{_v_frequency}</TableCell>
        <TableCell>{_v_priority}</TableCell>
      </TableRow>
    );
  }
})}
```

### 2. CS 응답 스크립트 테이블

**변경 전**:
```jsx
{_v_limited_scripts.map((script, index) => {
  if (typeof script === 'object' && script !== null) {
    return (
      <TableRow>
        <TableCell>{script.question || '질문'}</TableCell>
        <TableCell>{script.answer || '답변 없음'}</TableCell>
      </TableRow>
    );
  }
})}
```

**변경 후**:
```jsx
{_v_limited_scripts.map((script, index) => {
  // ✅ 문자열인 경우 JSON 파싱 시도
  let _v_parsed_script = script;
  if (typeof script === 'string') {
    try {
      _v_parsed_script = JSON.parse(script);
    } catch (e) {
      _v_parsed_script = { question: script, answer: '' };
    }
  }
  
  if (typeof _v_parsed_script === 'object' && _v_parsed_script !== null) {
    // ✅ 다양한 필드명 지원
    const _v_question = _v_parsed_script.question || 
                        _v_parsed_script.question_category || '질문';
    const _v_answer = _v_parsed_script.answer || 
                      _v_parsed_script.response || 
                      _v_parsed_script.response_template || '답변 없음';
    
    return (
      <TableRow>
        <TableCell>{index + 1}</TableCell>
        <TableCell>{_v_question}</TableCell>  {/* ← 질문만 표시 */}
        <TableCell>{_v_answer}</TableCell>    {/* ← 답변만 표시 */}
      </TableRow>
    );
  }
})}
```

---

## 📊 변경 전후 비교

### 변경 전 (JSON 원시 데이터 표시)
```
┌────┬──────────┬─────────────────────────────────────────────────────────────────────┬────────┬──────────┐
│순번│카테고리  │질문 내용                                                            │빈도    │우선순위  │
├────┼──────────┼─────────────────────────────────────────────────────────────────────┼────────┼──────────┤
│ 1  │    -     │{"category":"방송 일정","question":"라이브 방송은 언제 시작하나요?", │   -    │    -     │
│    │          │"frequency":"high","priority":1}                                      │        │          │
├────┼──────────┼─────────────────────────────────────────────────────────────────────┼────────┼──────────┤
│ 2  │    -     │{"category":"방송 일정","question":"방송 시간이 몇 시간 동안 진행되나│   -    │    -     │
│    │          │요?","frequency":"high","priority":2}                                 │        │          │
└────┴──────────┴─────────────────────────────────────────────────────────────────────┴────────┴──────────┘
```

### 변경 후 (파싱된 깔끔한 데이터 표시)
```
┌────┬──────────┬─────────────────────────────────────┬────────┬──────────┐
│순번│카테고리  │질문 내용                            │빈도    │우선순위  │
├────┼──────────┼─────────────────────────────────────┼────────┼──────────┤
│ 1  │방송 일정 │라이브 방송은 언제 시작하나요?       │ high   │    1     │
├────┼──────────┼─────────────────────────────────────┼────────┼──────────┤
│ 2  │방송 일정 │방송 시간이 몇 시간 동안 진행되나요? │ high   │    2     │
├────┼──────────┼─────────────────────────────────────┼────────┼──────────┤
│ 3  │방송 일정 │다시보기가 가능한가요?               │ high   │    3     │
└────┴──────────┴─────────────────────────────────────┴────────┴──────────┘
```

---

## 🔧 기술적 개선 사항

### 1. JSON 파싱 로직
```javascript
// 문자열인 경우 JSON 파싱 시도
let _v_parsed_question = question;
if (typeof question === 'string') {
  try {
    _v_parsed_question = JSON.parse(question);
  } catch (e) {
    // 파싱 실패시 원본 문자열 사용
    _v_parsed_question = { question: question };
  }
}
```

**장점**:
- ✅ 문자열 형태의 JSON 자동 파싱
- ✅ 파싱 실패시 안전한 폴백 처리
- ✅ 다양한 데이터 형태 지원

### 2. 다중 필드명 지원
```javascript
// 다양한 필드명 지원
const _v_category = _v_parsed_question.category || 
                    _v_parsed_question.question_category || '일반';

const _v_question = _v_parsed_script.question || 
                    _v_parsed_script.question_category || '질문';

const _v_answer = _v_parsed_script.answer || 
                  _v_parsed_script.response || 
                  _v_parsed_script.response_template || '답변 없음';
```

**장점**:
- ✅ 다양한 데이터 소스 호환
- ✅ 필드명 변경에 유연하게 대응
- ✅ 기본값 제공으로 안정성 향상

### 3. 에러 처리
```javascript
try {
  _v_parsed_question = JSON.parse(question);
} catch (e) {
  // 파싱 실패시 안전한 폴백
  _v_parsed_question = { question: question };
}
```

**장점**:
- ✅ JSON 파싱 실패시에도 앱 크래시 방지
- ✅ 원본 데이터 보존
- ✅ 사용자 경험 저하 최소화

---

## ✅ 완료 체크리스트

- [x] 예상 고객 질문 JSON 파싱 로직 추가
- [x] 예상 고객 질문 필드별 데이터 추출
- [x] CS 응답 스크립트 JSON 파싱 로직 추가
- [x] CS 응답 스크립트 필드별 데이터 추출
- [x] 다중 필드명 지원 (category, question_category 등)
- [x] 에러 처리 및 폴백 로직
- [x] 기본값 설정 (빈 데이터 대응)

---

## 🎉 완료!

CS 응대용 정보 테이블에서 JSON 형태의 원시 데이터가 **깔끔하게 파싱되어 표시**됩니다!

**주요 성과**:
- ✅ 가독성 대폭 향상 (JSON → 일반 텍스트)
- ✅ 사용자 친화적 표시
- ✅ 데이터 구조 변경에 유연한 대응
- ✅ 안정적인 에러 처리
- ✅ 다양한 필드명 지원

**변경 전**: `{"category":"방송 일정","question":"라이브 방송은 언제 시작하나요?","frequency":"high","priority":1}`

**변경 후**: `라이브 방송은 언제 시작하나요?` (카테고리: 방송 일정, 빈도: high, 우선순위: 1)
